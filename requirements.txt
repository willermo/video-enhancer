# --- Core AI Libraries ---
gfpgan==1.3.8
realesrgan==0.3.0

# --- PyTorch (GPU) ---
# We force these specific, known-compatible versions
torch==2.0.1
torchvision==0.15.2

# --- Dependencies ---
basicsr==1.4.2
facexlib==0.3.0

# --- Utilities & Critical Pins ---
numpy==1.26.4           # Pinned to 1.x for basicsr/torch
opencv-python==4.9.0.80  # Pinned to 4.9.x for NumPy 1.x compatibility
tqdm
